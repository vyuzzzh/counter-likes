// const blockButton = document.querySelector('.btn-group');

// if (blockButton) {
//   blockButton.addEventListener('click', async (event) => {
//     event.preventDefault();
//     const button = event.target.name;
//     console.log(event.target.name);

//     const response = await fetch(`/users/${button}`)
//     }

    // const name = event.target.name.value;
    // const email = event.target.email.value;
    // const password = event.target.password.value;
    // const {
    //   action, method,
    // } = event.target;

    // fetch(action, {
    //   method,
    //   headers: {
    //     'Content-Type': 'application/json',
    //   },
    //   body: JSON.stringify({
    //     name, email, password,
    //   }),
    // })
    //   .then((response) => response.json())
    //   .then((json) => {
    //     if (json.state) {
    //       window.location = '/';
    //       return true;
    //     }
    //     const divError = document.querySelector('.err');
    //     divError.innerText = json.answ;
    //   })
    //   .catch(() => window.alert('Ooops... Something went wrong.'));
//   });
// }
